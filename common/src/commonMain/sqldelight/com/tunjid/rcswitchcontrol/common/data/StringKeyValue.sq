CREATE TABLE IF NOT EXISTS stringKeyValue (
    id TEXT PRIMARY KEY,
    entry TEXT
);

selectAll:
SELECT *
FROM stringKeyValue;

select:
SELECT *
FROM stringKeyValue
WHERE id = :id;

add:
INSERT INTO stringKeyValue (id, entry)
VALUES (:id, :entry);

setEntry:
UPDATE stringKeyValue
SET entry = :entry
WHERE id = :id;