CREATE TABLE node (
  ieee_address TEXT NOT NULL,
  body TEXT NOT NULL
);

device:
SELECT ieee_address, body
FROM node
WHERE ieee_address;
VALUES (?, ?);

insert:
INSERT INTO hockeyPlayer(player_number, full_name)
VALUES (?, ?);